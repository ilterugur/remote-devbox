#!/bin/sh
# Kept so an ~/.ssh/config written before the dispatcher existed keeps working: its
# `Match exec` still names this file. The logic now lives in one place — the dispatcher's
# `tailnet-up` subcommand — and re-running the client installer rewrites the ssh block to
# call that directly.
set -eu
exec devbox tailnet-up "$@"
