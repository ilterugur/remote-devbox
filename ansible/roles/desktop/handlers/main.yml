---
- name: Restart xrdp
  ansible.builtin.systemd_service:
    name: "{{ item }}"
    state: restarted
  loop:
    - xrdp-sesman.service
    - xrdp.service

- name: Restart fail2ban
  ansible.builtin.systemd_service:
    name: fail2ban
    state: restarted
