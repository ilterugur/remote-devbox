[Unit]
Description=devbox fallback headless Chrome (CDP 127.0.0.1:{{ devbox_browser.failover.fallback_chrome_port }})
After=network.target

[Service]
Type=simple
User={{ devbox_browser.failover.chrome_user }}
Group={{ devbox_browser.failover.chrome_user }}
Environment=HOME=/home/{{ devbox_browser.failover.chrome_user }}
ExecStart=/usr/bin/google-chrome --headless=new --remote-debugging-port={{ devbox_browser.failover.fallback_chrome_port }} --remote-debugging-address=127.0.0.1 --remote-allow-origins=* --user-data-dir=/home/{{ devbox_browser.failover.chrome_user }}/.cache/devbox-fallback-chrome --no-sandbox --disable-gpu --disable-dev-shm-usage --no-first-run --no-default-browser-check --window-size=1440,900
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
