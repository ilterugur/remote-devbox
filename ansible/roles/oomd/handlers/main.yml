---
# A slice drop-in needs a daemon-reload to be seen; oomd re-reads slice properties from
# systemd, so it does not need restarting when only a drop-in changes.
- name: Reload systemd
  ansible.builtin.systemd_service:
    daemon_reload: true
